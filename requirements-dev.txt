# CodeCortex — development & test dependencies
-r requirements.txt

pytest>=8.0
pytest-cov>=4.0
# Pinned to a minor: ruff's formatter output changes between minors (0.16 began
# formatting Python blocks inside Markdown), which turns `--check` in CI into a
# failure nobody changed anything to cause.
ruff>=0.16,<0.17
mypy>=1.8

# Semantic indexing (used in integration tests)
jedi>=0.19

# Clustering (used in Phase 5 tests)
hdbscan>=0.8
umap-learn>=0.5.12
scikit-learn>=1.3
